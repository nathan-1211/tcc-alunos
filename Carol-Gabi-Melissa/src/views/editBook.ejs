<!DOCTYPE html>
<html lang="pt-br">
  <%- include('components/head') %>
  <body>
    <main>
      <%- include('components/header') %>
      <div class="content-form-cadastro">
        <h1>Atualizar Livro</h1>
        <form
          action="/registerBooks/updateBook/<%= book.book_id %>"
          method="post"
          enctype="multipart/form-data"
        >
          <div class="left">
            <div class="input input-book-image">
              <label for="book_image" class="image-preview">
                <!-- <span>Escolha a imagem do Livro</span> -->
                <img id="img-book" src="<%= book.book_image %>" alt="" />
              </label>
              <input
                type="file"
                name="book_image"
                class="book_image"
                id="book_image"
                accept="image/*"
              />
              <input
                type="hidden"
                name="book_image"
                value="<%= book.book_image %>"
              />
            </div>
          </div>
          <div class="right">
            <div class="input input-book-name">
              <label for="book_name"></label>
              <input
                type="text"
                name="book_name"
                class="book_name"
                placeholder="Digite o nome do Livro"
                id="book_name"
                value="<%= book.book_name %>"
              />
            </div>
            <div class="input input-book-autor">
              <label for="book_autor"></label>
              <input
                type="text"
                name="book_autor"
                class="book_autor"
                placeholder="Digite o nome do Autor ou Autora"
                id="book_autor"
                value="<%= book.book_autor %>"
              />
            </div>
            <div class="input input-book-categoria">
              <label for="book_categoria"></label>
              <input
                type="text"
                name="book_categoria"
                class="book_categoria"
                placeholder="Digite uma categoria"
                id="book_categoria"
                value="<%= book.book_categoria %>"
              />
            </div>
            <div class="input input-book-desc">
              <label for="book_desc"></label>
              <textarea
                name="book_desc"
                class="book_desc"
                placeholder="Digite a descrição do Livro"
                id="book_desc"
              >
<%= book.book_desc %>
            </textarea
              >
            </div>
            <div class="content-btn">
              <button>Atualizar</button>
              <a href="/home">Cancelar a Edição</a>
            </div>
            <% if(msgSuccess){%>
            <div class="msgSuccess"><%= msgSuccess %></div>
            <% } %> <% if(msgError){%>
            <div class="msgError"><%= msgError %></div>
            <% } %>
          </div>
        </form>
      </div>
      <%- include('components/footer') %>
    </main>
    <%- include('components/script') %>
  </body>
</html>
